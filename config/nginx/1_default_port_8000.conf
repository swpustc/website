## HTTP host
server {
  listen 0.0.0.0:8000 default_server;
  listen [::]:8000 ipv6only=on default_server;
  server_name _;
  server_tokens off; ## Don't show the nginx version number, a security best practice
  root /home/git/html/port_8000;

  ## Increase this if you want to upload large attachments
  ## Or if you want to accept large objects over http
  client_max_body_size 500m;

  access_log  /var/log/nginx/default_8000_access.log proxy;
  error_log   /var/log/nginx/default_8000_error.log;

  location / {
    index off;
    try_files $uri =502;
  }

  include /home/git/html/config/nginx/global/filter.conf;
  include /home/git/html/config/nginx/global/deny.conf;
  include /home/git/html/config/nginx/global/expires.conf;

  error_page 400 /400.html;
  error_page 403 404 414 422 500 502 503 504 =502 /502.html;
}
